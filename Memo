시험 - 변수명 숫자로 시작하면 안됨!!!!!! ⭐️

# ... => 글자 크게
+ ... => •

Install = !pip install ____

함수 정의= def ____ 

정의의 내용은 다음 줄에 indent

따옴표 = 텍스트 인식 (‘ ‘ “ ”)

( ) = 함수

bulit in = 내장된 함수

Colab 저장 = 파일 들어가서 Github 사본 저장

Comment # ctrl + /
작동 ctrl + 엔터
print (변수) 
type (변수)

ChatGPT = Generative Pre-trained Transformer ⭐️

APIs = Application Programming Interfaces ⭐️

type( ) => 숫자 타입 알려줌... 

[ ] => List (Python은 0부터 셈)

str( ) => “ “ (string)
int( ) => 숫자
[ ] => 괄호 안이 숫자가 될 수도 있고 str이 될 수도 있음
list[x숫자] => 몇 번째까지 보여줌... 0부터 셈

range( ) => 다 보여줘라
왼쪽은 들어가고 오른쪽은 안 들어감

{ } => Dictionary datatype

Input( ) => 팝업

*=> 곱하기

import => 불러오기

input => 사용자가 입력

input( ) => 기본적으로 string

sentence.split( ) => 문장 분할

uppercase_text = text.upper( )
=> 대문자로 바꿈 
lowercase_text = text.lower( )
=> 소문자로 바꿈

String indexing
print(quote[ ]) => 몇 번째 글자 나옴 (0부터 셈)
맨끝은 -1 ... 빈칸이나 온점도 포함해서 셈

숫자:숫자 => 왼쪽은 해당 숫자 포함
오른쪽은 해당 숫자 포함 X
비워두면 끝까지 셈

:: 숫자 => reverse

' ' join( ) => 다시 문장으로 붙임

len( ) => 길이

for loop => 반복
for num in numbers
** => n의 제곱
squared[ ]
squared.append( ) => 괄호 안에 결과값 넣음
